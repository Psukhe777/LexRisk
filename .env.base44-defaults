# Placeholder credentials so the API boots before real keys exist.
# Real values arrive via the Base44 secrets store and override these.
# NOT secrets — never put a real key in this file.
GROQ_API_KEY=placeholder-not-a-real-key
